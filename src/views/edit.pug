extends base.pug

block content
    h4 Update Video
    form(method="POST")
        input(name="title", placeholder="Video Title", value=video.title, required)
        input(placeholder="Description", required, type="text" name="description" value=video.description)
        input(placeholder="Hashtags, separated by comma.", required, type="text" name="hashtags" value=video.hashtags.join())
        input(value="save", type="submit")


//- get request / post request의 차이점
//- post 방식은 파일을 보내거나 , database에 있는 값을 바꾸는 뭔가를 보낼 때 사용
//- get 방식을 쓰는 form은 어떨때 사용하는?
//- 예를 들어 구글이나 네이버에서 무언가 검색을 할 때 그 검색어가 주소창에 포함되어 있음
//- method : form과 back end 사이의 정보 전송에 관한 방식

//- input에 name 설정을 해주지 않으면 데이터가 전송되지 않는다
