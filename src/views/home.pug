extends base
include mixins/video
    
block content
    //- videos arr 안의 각 ele에 대해서 list item을 만들어서 arr 안에 넣어주고 있다
    //- mixin 안에 넣어줌
    div.video-grid
        each video in videos
            +video(video)
        else 
            span.empty__message 비디오가 없어요😂 업로드 부탁드립니다