extends base

block content
    if error_message
        span=error_message
    form(method="POST")
        input(name="username", type="text", required, placeholder="아이디를 입력하여 주세요")
        input(name="password", type="password", required, placeholder="비밀번호를 입력하여 주세요")
        input(type="submit", value="로그인")
        br
        a(href="https://github.com/login/oauth/authorize?client_id=4863668c3b3ef4d5d112&scope") github 로그인 하러가기
    hr
    div 
        span 회원이 아니신가요? 
        a(href="/join") 회원가입